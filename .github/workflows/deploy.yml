name: Hexo Deploy
<<<<<<< HEAD
on:
  push:
    branches: [ main, master ]
=======
concurrency:
  group: "pages"
  cancel-in-progress: true
on:
  push:
    branches: [ master ]
>>>>>>> 80f7f53e77b0634cc251e6a18c4c7efea1ca57b9
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      # 拉取完整源码
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 安装 Node 环境
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 1. 全局安装 hexo-cli（无需 package.json）
      - name: Install Hexo CLI global
        run: npm install hexo-cli -g

      # 2. 校验本地是否存在 package.json，不存在直接抛出提示
      - name: Check package.json
        run: |
          if [ ! -f "package.json" ]; then
            echo "ERROR: 仓库根目录缺少 package.json，请上传完整Hexo项目源码"
            exit 1
          fi

      # 3. 安装本地项目依赖（仅当 package.json 存在时执行）
      - name: Install local dependencies
        run: npm install

      # 构建静态页面
      - name: Build static files
        run: hexo clean && hexo generate

      # 部署到 gh-pages
      - name: Deploy to GitHub Pages
=======
      - name: 拉取完整源码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 配置Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: 安装项目依赖
        run: npm install
      - name: Hexo 构建静态文件
        run: npx hexo clean && npx hexo generate
      - name: 部署到 gh-pages
>>>>>>> 80f7f53e77b0634cc251e6a18c4c7efea1ca57b9
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
<<<<<<< HEAD
          publish_branch: gh-pages
=======
          publish_branch: gh-pages
>>>>>>> 80f7f53e77b0634cc251e6a18c4c7efea1ca57b9
